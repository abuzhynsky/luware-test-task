<UserControl x:Class="LyncStatusChecker.SL.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="50" d:DesignWidth="400"
    DataContext="{Binding Source={StaticResource Locator}, Path=Main}">

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" Text="{Binding SipUri, Mode=TwoWay}" HorizontalAlignment="Center" VerticalAlignment="Top" Width="100" Height="50" />
        <Button Grid.Column="1" Command="{Binding GetPresenceCommand}" Content="Get Presence" HorizontalAlignment="Center" Height="50" VerticalAlignment="Top" Width="100"/>
        <TextBlock Grid.Column="2" Text="{Binding State, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Top" Height="50" Width="100" />
    </Grid>
</UserControl>
